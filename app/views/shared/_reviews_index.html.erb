<h1>This will be the partial to render in the client info show page</h1>

<h2 class="h2">
  This are the reviews for this client ⬇️
</h2>
<div>
  <% @reviews.each do |review| %>
    <p><%= review.comment %></p>
    <p><%= review.punctuality %></p>
    <p><%= review.attendance %></p>
    <%#= reference the booking to which the review belonged  %>
  <% end %>
</div>

<div>
  <h3>Here are the reviews of the client that happened on our restaurant:</h3>
  <% my_restaurant_bookings.each do |booking| %>
    <p><%= booking.client.reviews.where(booking: booking) %></p>
  <% end %>
</div>
<!--
my_restaurant_client_reviews = []

 my_restaurant_bookings.each do |booking|
my_restaurant_client_reviews << booking.client.reviews.where(booking: booking)
end
-->
