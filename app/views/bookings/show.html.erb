  <div class="container content-container">
    <div class="d-flex justify-content-between">

      <div class="m-7 gep-top mt-4">
        <h1><%= @booking.client.first_name%> <%= @booking.client.last_name%>'s Booking | CCR - <%= @booking.client.ccr %></h1>
        <br>
        <h3> 📞<%= @booking.client.phone_number %></h3>
        <br>
        <h3>🕜 <%= @booking.booking_start.strftime('%I:%M') %> | <%= @booking.booking_start.strftime('%b %d, %Y') %> </h3>
        <br>
        <h4>👥 <%= @booking.party_count %></h4>
        <br>
        <h3>Table nº: <%= rand(20..60) %></h3>
        <br>
        <br>
        <h4>Comment:</h4>
        <p><%= @booking.comment %></p>
          <div class="m-7 gep-top mt-4 row align-items-start">
            <div class="col"><p><%= link_to "Client Details", client_path(@booking.client), class:"btn btn-primary" %></p> </div>
            <div class="col"> <p><%= link_to "Update Booking", edit_booking_path(@booking), class:"btn btn-info" %></p> </div>
            <div class="col"> <p><%= link_to "Delete Booking", booking_path(@booking), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: 'btn btn-danger' %></p> </div>
          </div>
      </div>

    </div>
  </div>
